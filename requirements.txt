numpy>=1.24
opencv-python-headless>=4.8
# Allen-Bradley PLC push (EtherNet/IP). Optional: only needed for --plc-ip.
pycomm3>=1.2
# Industrial HMI GUI. Optional: only needed for `terminaltorque-hmi`.
PySide6>=6.6
# Basler camera support. Optional: only needed for the Basler (pypylon) source.
pypylon>=3.0
